def calculate():
    a = 10
    b = 1                     # ← вместо 0, чтобы избежать деления на ноль
    result = a / b            # теперь работает

    numbers = [1, 2, 3]
    print(numbers[0])         # ← индекс 0 существует (выводит 1)

    return result

# Неиспользуемая переменная удалена

# Теперь всё работает
print("Результат:", calculate())